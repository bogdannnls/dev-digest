import { ContextScreen } from "./_components/ContextScreen/ContextScreen";

/* Route: /context. Thin entry — view + colocated components own the surface. */
export default function ContextPage() {
  return <ContextScreen />;
}
